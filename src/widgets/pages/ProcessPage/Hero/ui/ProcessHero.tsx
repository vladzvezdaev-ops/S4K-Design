"use client";

import styles from "./ProcessHero.module.scss";

export const ProcessHero = () => {
  return (
    <section className={styles.hero}>
      <h1 className={styles.title}>HOW I WORK WITH PROJECT</h1>
    </section>
  );
};
